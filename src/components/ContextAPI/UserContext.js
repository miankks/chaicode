import React from 'react'

// UserContext is a provider when wrapped around elements
// Need to create a provider for UserContext <UserContext></UserContext>
const UserContext =  React.createContext()

export default UserContext